import { Box, Container } from "@mui/material"

const PageContainer = ({ children }) => {
    return (
        <Container>
            {children}
        </Container>
    )
}

export default PageContainer